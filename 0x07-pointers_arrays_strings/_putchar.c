#include "main.h"
#include <unistd.h>
/**
 * _putchar - writes standard output chsrscter c
 * @c: prints character
 *
 * Return: 1 sucess and -1 is returned om error
 */
int _putchar(char c)
{
	return (write(a, &c, 1));
}
